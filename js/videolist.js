"use strict"
// const request = require('request')
// const cheerio = require('cheerio')
const fs = require('fs')

var path = "../video"
fs.readdir(path, function(err, files) {
	if (err) throw err;
	log("files")
})

// var videoNames = fs.readdirSync(path)
